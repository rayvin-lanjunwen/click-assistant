<!--
    Click Assistant 无障碍服务配置

    权限说明：
    - 本服务仅在你主动启动任务后执行你已经配置好的手势（点击、滑动、文本输入）。
    - 不会自动触发、不会读取或记录任何屏幕内容，也不会在后台持续监听。
    - 文本输入仅使用无障碍节点权限写入文本内容，不会读取已有文本或用户密码。

    事件类型说明：
    - typeWindowStateChanged：用于在执行文本输入时获取当前聚焦的输入框。
    - typeWindowContentChanged：辅助确认目标界面窗口切换后的可交互状态。
    - 不使用 typeAllMask，避免监听不必要的系统事件。

    notificationTimeout=500：减少高频事件回调，降低系统开销。
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="500" />
